# QA Audit Task 020

Task: `[P1][factory_agent_20_cost] Routes cheap deterministic work to Python and expensive judgement to LLMs.`

Profile: strict

## Scope Reviewed

- `README.md`
- `factory-prompt.md`
- `.vscode/settings.json`
- `.codex-loop/factory/cost-routing-contract.json`
- `.codex-loop/factory/cost-routing-scorecard.json`
- `.codex-loop/factory/cost-routing-policy.md`
- `tools/validate_copilot_factory.py`
- Generated validation, prompt quality, runtime equivalence and documentation audit reports.

## Findings

1. Fixed: documentation audit coverage missed the new cost-routing operator policy.
   - Evidence: `generated/documentation-audit-report.json` listed README, operating system docs, factory prompt and operate runbooks, but did not list `.codex-loop/factory/cost-routing-policy.md`.
   - Risk: the new operator policy could drift while `docsAuditor` still reported pass, weakening release traceability for the cost-routing mission.
   - Patch: added `COST_POLICY_DOC` to `DOCS_OPERATOR_DOCS` in `tools/validate_copilot_factory.py` with required markers for `# Cost Routing Policy`, `## Decision Contract`, `## Runtime Equivalence` and `## Verification`.
   - Result: regenerated documentation and validation reports now include `.codex-loop/factory/cost-routing-policy.md` in `operatorDocsChecked` and mark it pass.

2. No product UI flow issues found.
   - This increment is a local factory contract and deterministic validation surface, not a browser UI. UX checklist items were mapped to operator clarity, command discoverability and error states.

3. Empty/error state verified for the semantic router.
   - `python tools/semantic_router.py` exits non-zero with usage text for empty input.
   - `python tools/semantic_router.py python ci routing` returns Python as the top route with `cheap_path=true` and `routing_evidence.llm_assist_used=false`.

## Patches Made

- `tools/validate_copilot_factory.py`
  - Added `.codex-loop/factory/cost-routing-policy.md` to the deterministic documentation auditor contract.

- Regenerated by validation:
  - `generated/validation-report.json`
  - `generated/validation-report.md`
  - `generated/documentation-audit-report.json`
  - `generated/documentation-audit-report.md`

## Commands Executed

- `Get-Command rg -ErrorAction SilentlyContinue` - `rg` shim present.
- `git status --short` - failed because this workspace is not a Git repository.
- `python tools/validate_copilot_factory.py` - pass.
- `python tools/validate_prompt_quality.py` - pass.
- `python tools/validate_runtime_equivalence.py` - pass.
- `python tools/semantic_router.py python ci routing` - pass, top route `python`, cheap path, no LLM assist.
- `python tools/semantic_router.py` - expected failure for empty input, usage message returned.
- `python -m py_compile tools/validate_copilot_factory.py` - pass, but created Python bytecode cache.
- `python tools/validate_copilot_factory.py` after `py_compile` - failed as expected because release artifacts cannot contain `tools/__pycache__/`.
- Local cleanup of `tools/__pycache__/validate_copilot_factory.cpython-314.pyc` and the empty `tools/__pycache__` directory using resolved workspace paths.
- Final `python tools/validate_copilot_factory.py` - pass.
- Final `python tools/validate_prompt_quality.py` - pass.

## Residual Risks

- No Python bytecode cache remains after cleanup; `Get-ChildItem -Recurse -Force -Directory -Filter __pycache__` returned no entries.
- No Git metadata is present in `NuevoProyecto`; validation relies on `.codex-loop/run.lock.json` and `.codex-loop/factory/snapshots`, both reported valid by `generated/validation-report.json#/controlRoom`.
- LLM judgement quality remains an accepted residual risk in `.codex-loop/factory/cost-routing-scorecard.json`; the mitigation is that LLM judgement cannot replace Python validation, runtime equivalence or report-backed traceability.
