# Task 028 Department Council - Copiloto de Discovery AS-IS

Scope: `dist/copilots/as_is_discovery/*` runtime-contract review for Codex, Claude, GitHub Copilot and LangChain.

Verification run:

- `python tools/validate_copilot_factory.py` - PASS
- `python tools/validate_prompt_quality.py` - PASS
- `python tools/validate_runtime_equivalence.py` - PASS

| Departamento | PASS-FAIL | Evidencia | Cambios | Riesgos |
|---|---|---|---|---|
| Product | PASS | `shared/spec.json` keeps mission on legacy discovery, inventory and AS-IS documentation; declared outputs remain `as_is_report` and `inventory_json`. | No product-scope expansion. | Residual: live GitHub evidence remains pending when `github_mcp` is not activated. |
| Engineering | FAIL -> PASS | Codex/Claude/GitHub prompts referenced the full `output_schema.json` runtime parity rule, but their visible JSON envelope stopped at `cloud_migration`. | Added `runtime_contract` `$ref` to `codex/AGENT.md`, `claude/AGENT.md` and `github-copilot/copilot-agent.md` so prompt examples point to the shared contract without duplicating schema text. | Residual: Markdown adapters still depend on the shared schema reference being respected by operators. |
| Web/UI/Design | PASS | No UI, navigation, render or responsive surface exists in the scoped files; this is a runtime prompt/schema package. | None. | None for this task scope. |
| Creative Studio | PASS | No image, mockup, motion, pitch deck or social asset surface exists in the scoped files. | None. | None for this task scope. |
| QA | PASS | Required validators pass after patch; prompt quality report lists AS-IS runtimes within budget: Codex 12033 chars, Claude 11940, GitHub Copilot 12321, LangChain 15833. | Re-ran all three validation commands after edits. | Residual: validators prove static/runtime contract parity, not live connector behavior. |
| Safe-coding/Privacy | PASS | `mcp-placeholders.json` has `defaultEnabled: false`, stores no credential values and uses env name `GITHUB_TOKEN`; validation report shows Security Auditor, MCP Connector Auditor and Runtime Safety PASS. | None. | Residual: connector activation must stay operator-approved and use placeholders only. |
| Growth/SEO/Content | PASS | No landing, SEO metadata, blog, copy funnel or marketing claim surface exists in scope. | None. | None for this task scope. |
| Legal/Risk | PASS | Prompts prohibit fake inspected files, fake CI results and storing secrets; connector declarations are capability contracts, not credential grants. | None. | Residual: external repository evidence cannot be claimed until connector evidence is actually available. |
| Packaging/Release | PASS | `validate_copilot_factory.py`, `validate_prompt_quality.py` and `validate_runtime_equivalence.py` all pass; generated reports show AS-IS checked across 4 runtimes via `dist/copilots/as_is_discovery/shared/spec.json`. | Validation reports regenerated by the verification commands. | Residual: no Git metadata is available in this workspace, so release diff trace relies on this audit artifact and validator receipts. |
| Commercial/Finance | PASS | No pricing, billing, customer data or sales promise surface exists in the scoped runtime contract. | None. | None for this task scope. |

Council decision: PASS after patch. The only concrete defect was prompt/example drift around the `runtime_contract` surface; it was patched in the three Markdown runtime adapters and verified without inflating prompt cost beyond the quality gate.
