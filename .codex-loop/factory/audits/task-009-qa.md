# QA Audit Task 009

Task: `[P0][factory_agent_09_security] Audits sensitive credentials policy, threat model and safe MCP usage.`

Profile: strict

## Scope Audited

- `config/.env.example`
- `config/mcp-connectors.example.json`
- `tools/validate_copilot_factory.py`
- generated validation and prompt-quality reports
- related regeneration surface in `tools/generate_copilot_factory.py`
- operator documentation in `README.md`

## Findings

1. Release-readiness regression found and fixed.
   `tools/generate_copilot_factory.py` still contained the previous MCP example template with GitHub enabled by default and a broad read/write scope. Running the documented generator command could overwrite the new safe MCP policy.

2. Documentation gap found and fixed.
   `README.md` listed generated runtime artifacts but did not list the new credential/MCP policy surface or the placeholder-only rule.

3. No product UI was affected.
   Checklist items for responsive UX, loading states and form usability are not applicable to this increment because the audited surface is config plus Python validation.

## Patches Made

- Updated `tools/generate_copilot_factory.py` so regeneration writes the same disabled-by-default MCP policy, threat model, runtime equivalence contract and placeholder-only env example as the audited config files.
- Added generator-level checks in `tools/validate_copilot_factory.py`:
  - `generatedSecurityPolicyChecked`
  - `generatedSecurityEnvChecked`
- Updated the validation markdown renderer to expose the new generator checks.
- Updated `README.md` with the credential/MCP policy artifacts and operator rule.
- Removed Python bytecode generated by local compile verification from `tools/__pycache__`.

## Commands Executed

- `Get-Command rg -ErrorAction SilentlyContinue`
  - Pass: `rg` shim available.
- `git status --short`
  - Not applicable: workspace is not a Git repository.
- `python -m py_compile tools/generate_copilot_factory.py tools/validate_copilot_factory.py`
  - Pass.
- `rg -n 'enabled": true|read/write as allowed|"scope":' config tools/generate_copilot_factory.py`
  - Pass: no unsafe MCP default/template remains.
- `python tools/validate_copilot_factory.py`
  - Pass: 18 copilots, 50 agents, 50 tasks.
- `python tools/validate_prompt_quality.py`
  - Pass: 18 copilots, 72 runtime prompts.

## Residual Risks

- `data/agent_roster.json` and `generated/agent-roster.json` still use the older mission wording "Audits secrets policy..." for the security agent. The validator explicitly treats it as equivalent to the sensitive-credentials wording, so this is traceability noise rather than a blocking defect.
- No live MCP connection was exercised. This audit verifies repository policy, examples, negative cases and regeneration safety only.
